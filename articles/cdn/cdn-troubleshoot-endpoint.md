---
title: Troubleshooting Azure CDN endpoints returning 404 status | Microsoft Docs
description: Troubleshoot 404 response codes with Azure CDN endpoints.
services: cdn
documentationcenter: ''
author: zhangmanling
manager: erikre
editor: ''
ms.assetid: b588a1eb-ab69-4fc7-ae4d-157c3e46f4a8
ms.service: cdn
ms.workload: tbd
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 01/23/2017
ms.author: mazha
ms.openlocfilehash: bcbdb0b32800255f0bfaaff34b5e814d3580c9cf
ms.sourcegitcommit: 5b9d839c0c0a94b293fdafe1d6e5429506c07e05
ms.translationtype: HT
ms.contentlocale: nl-NL
ms.lasthandoff: 08/02/2018
ms.locfileid: "44663051"
---
# <a name="troubleshooting-cdn-endpoints-returning-404-statuses"></a><span data-ttu-id="b33db-103">Troubleshooting CDN endpoints returning 404 statuses</span><span class="sxs-lookup"><span data-stu-id="b33db-103">Troubleshooting CDN endpoints returning 404 statuses</span></span>
<span data-ttu-id="b33db-104">This article helps you troubleshoot issues with [CDN endpoints](cdn-create-new-endpoint.md) returning 404 errors.</span><span class="sxs-lookup"><span data-stu-id="b33db-104">This article helps you troubleshoot issues with [CDN endpoints](cdn-create-new-endpoint.md) returning 404 errors.</span></span>

<span data-ttu-id="b33db-105">If you need more help at any point in this article, you can contact the Azure experts on [the MSDN Azure and the Stack Overflow forums](https://azure.microsoft.com/support/forums/).</span><span class="sxs-lookup"><span data-stu-id="b33db-105">If you need more help at any point in this article, you can contact the Azure experts on [the MSDN Azure and the Stack Overflow forums](https://azure.microsoft.com/support/forums/).</span></span> <span data-ttu-id="b33db-106">Alternatively, you can also file an Azure support incident.</span><span class="sxs-lookup"><span data-stu-id="b33db-106">Alternatively, you can also file an Azure support incident.</span></span> <span data-ttu-id="b33db-107">Go to the [Azure Support site](https://azure.microsoft.com/support/options/) and click on **Get Support**.</span><span class="sxs-lookup"><span data-stu-id="b33db-107">Go to the [Azure Support site](https://azure.microsoft.com/support/options/) and click on **Get Support**.</span></span>

## <a name="symptom"></a><span data-ttu-id="b33db-108">Symptom</span><span class="sxs-lookup"><span data-stu-id="b33db-108">Symptom</span></span>
<span data-ttu-id="b33db-109">You've created a CDN profile and an endpoint, but your content doesn't seem to be available on the CDN.</span><span class="sxs-lookup"><span data-stu-id="b33db-109">You've created a CDN profile and an endpoint, but your content doesn't seem to be available on the CDN.</span></span>  <span data-ttu-id="b33db-110">Users who attempt to access your content via the CDN URL receive HTTP 404 status codes.</span><span class="sxs-lookup"><span data-stu-id="b33db-110">Users who attempt to access your content via the CDN URL receive HTTP 404 status codes.</span></span> 

## <a name="cause"></a><span data-ttu-id="b33db-111">Cause</span><span class="sxs-lookup"><span data-stu-id="b33db-111">Cause</span></span>
<span data-ttu-id="b33db-112">There are several possible causes, including:</span><span class="sxs-lookup"><span data-stu-id="b33db-112">There are several possible causes, including:</span></span>

* <span data-ttu-id="b33db-113">The file's origin isn't visible to the CDN</span><span class="sxs-lookup"><span data-stu-id="b33db-113">The file's origin isn't visible to the CDN</span></span>
* <span data-ttu-id="b33db-114">The endpoint is misconfigured, causing the CDN to look in the wrong place</span><span class="sxs-lookup"><span data-stu-id="b33db-114">The endpoint is misconfigured, causing the CDN to look in the wrong place</span></span>
* <span data-ttu-id="b33db-115">The host is rejecting the host header from the CDN</span><span class="sxs-lookup"><span data-stu-id="b33db-115">The host is rejecting the host header from the CDN</span></span>
* <span data-ttu-id="b33db-116">The endpoint hasn't had time to propagate throughout the CDN</span><span class="sxs-lookup"><span data-stu-id="b33db-116">The endpoint hasn't had time to propagate throughout the CDN</span></span>

## <a name="troubleshooting-steps"></a><span data-ttu-id="b33db-117">Troubleshooting steps</span><span class="sxs-lookup"><span data-stu-id="b33db-117">Troubleshooting steps</span></span>
> [!IMPORTANT]
> <span data-ttu-id="b33db-118">After creating a CDN endpoint, it will not immediately be available for use, as it takes time for the registration to propagate through the CDN.</span><span class="sxs-lookup"><span data-stu-id="b33db-118">After creating a CDN endpoint, it will not immediately be available for use, as it takes time for the registration to propagate through the CDN.</span></span>  <span data-ttu-id="b33db-119">For <b>Azure CDN from Akamai</b> profiles, propagation usually completes within one minute.</span><span class="sxs-lookup"><span data-stu-id="b33db-119">For <b>Azure CDN from Akamai</b> profiles, propagation usually completes within one minute.</span></span>  <span data-ttu-id="b33db-120">For <b>Azure CDN from Verizon</b> profiles, propagation will usually complete within 90 minutes, but in some cases can take longer.</span><span class="sxs-lookup"><span data-stu-id="b33db-120">For <b>Azure CDN from Verizon</b> profiles, propagation will usually complete within 90 minutes, but in some cases can take longer.</span></span>  <span data-ttu-id="b33db-121">If you complete the steps in this document and you're still getting 404 responses, consider waiting a few hours to check again before opening a support ticket.</span><span class="sxs-lookup"><span data-stu-id="b33db-121">If you complete the steps in this document and you're still getting 404 responses, consider waiting a few hours to check again before opening a support ticket.</span></span>
> 
> 

### <a name="check-the-origin-file"></a><span data-ttu-id="b33db-122">Check the origin file</span><span class="sxs-lookup"><span data-stu-id="b33db-122">Check the origin file</span></span>
<span data-ttu-id="b33db-123">First, we should verify the that the file we want cached is available on our origin and is publicly accessible.</span><span class="sxs-lookup"><span data-stu-id="b33db-123">First, we should verify the that the file we want cached is available on our origin and is publicly accessible.</span></span>  <span data-ttu-id="b33db-124">The quickest way to do that is to open a browser in an In-Private or Incognito session and browse directly to the file.</span><span class="sxs-lookup"><span data-stu-id="b33db-124">The quickest way to do that is to open a browser in an In-Private or Incognito session and browse directly to the file.</span></span>  <span data-ttu-id="b33db-125">Just type or paste the URL into the address box and see if that results in the file you expect.</span><span class="sxs-lookup"><span data-stu-id="b33db-125">Just type or paste the URL into the address box and see if that results in the file you expect.</span></span>  <span data-ttu-id="b33db-126">For this example, I'm going to use a file I have in an Azure Storage account, accessible at `https://cdndocdemo.blob.core.windows.net/publicblob/lorem.txt`.</span><span class="sxs-lookup"><span data-stu-id="b33db-126">For this example, I'm going to use a file I have in an Azure Storage account, accessible at `https://cdndocdemo.blob.core.windows.net/publicblob/lorem.txt`.</span></span>  <span data-ttu-id="b33db-127">As you can see, it successfully passes the test.</span><span class="sxs-lookup"><span data-stu-id="b33db-127">As you can see, it successfully passes the test.</span></span>

![Success!](https://docstestmedia1.blob.core.windows.net/azure-media/articles/cdn/media/cdn-troubleshoot-endpoint/cdn-origin-file.png)

> [!WARNING]
> <span data-ttu-id="b33db-129">While this is the quickest and easiest way to verify your file is publicly available, some network configurations in your organization could give you the illusion that this file is publicly available when it is, in fact, only visible to users of your network (even if it's hosted in Azure).</span><span class="sxs-lookup"><span data-stu-id="b33db-129">While this is the quickest and easiest way to verify your file is publicly available, some network configurations in your organization could give you the illusion that this file is publicly available when it is, in fact, only visible to users of your network (even if it's hosted in Azure).</span></span>  <span data-ttu-id="b33db-130">If you have an external browser from which you can test, such as a mobile device that is not connected to your organization's network, or a virtual machine in Azure, that would be best.</span><span class="sxs-lookup"><span data-stu-id="b33db-130">If you have an external browser from which you can test, such as a mobile device that is not connected to your organization's network, or a virtual machine in Azure, that would be best.</span></span>
> 
> 

### <a name="check-the-origin-settings"></a><span data-ttu-id="b33db-131">Check the origin settings</span><span class="sxs-lookup"><span data-stu-id="b33db-131">Check the origin settings</span></span>
<span data-ttu-id="b33db-132">Now that we've verified the file is publicly available on the internet, we should verify our origin settings.</span><span class="sxs-lookup"><span data-stu-id="b33db-132">Now that we've verified the file is publicly available on the internet, we should verify our origin settings.</span></span>  <span data-ttu-id="b33db-133">In the [Azure Portal](https://portal.azure.com), browse to your CDN profile and click the endpoint you're troubleshooting.</span><span class="sxs-lookup"><span data-stu-id="b33db-133">In the [Azure Portal](https://portal.azure.com), browse to your CDN profile and click the endpoint you're troubleshooting.</span></span>  <span data-ttu-id="b33db-134">In the resulting **Endpoint** blade, click the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-134">In the resulting **Endpoint** blade, click the origin.</span></span>  

![Endpoint blade with origin highlighted](https://docstestmedia1.blob.core.windows.net/azure-media/articles/cdn/media/cdn-troubleshoot-endpoint/cdn-endpoint.png)

<span data-ttu-id="b33db-136">The **Origin** blade appears.</span><span class="sxs-lookup"><span data-stu-id="b33db-136">The **Origin** blade appears.</span></span> 

![Origin blade](https://docstestmedia1.blob.core.windows.net/azure-media/articles/cdn/media/cdn-troubleshoot-endpoint/cdn-origin-settings.png)

#### <a name="origin-type-and-hostname"></a><span data-ttu-id="b33db-138">Origin type and hostname</span><span class="sxs-lookup"><span data-stu-id="b33db-138">Origin type and hostname</span></span>
<span data-ttu-id="b33db-139">Verify the **Origin type** is correct, and verify the **Origin hostname**.</span><span class="sxs-lookup"><span data-stu-id="b33db-139">Verify the **Origin type** is correct, and verify the **Origin hostname**.</span></span>  <span data-ttu-id="b33db-140">In my example, `https://cdndocdemo.blob.core.windows.net/publicblob/lorem.txt`, the hostname portion of the URL is `cdndocdemo.blob.core.windows.net`.</span><span class="sxs-lookup"><span data-stu-id="b33db-140">In my example, `https://cdndocdemo.blob.core.windows.net/publicblob/lorem.txt`, the hostname portion of the URL is `cdndocdemo.blob.core.windows.net`.</span></span>  <span data-ttu-id="b33db-141">As you can see in the screenshot, this is correct.</span><span class="sxs-lookup"><span data-stu-id="b33db-141">As you can see in the screenshot, this is correct.</span></span>  <span data-ttu-id="b33db-142">For Azure Storage, Web App, and Cloud Service origins, the **Origin hostname** field is a dropdown, so we don't need to worry about spelling it correctly.</span><span class="sxs-lookup"><span data-stu-id="b33db-142">For Azure Storage, Web App, and Cloud Service origins, the **Origin hostname** field is a dropdown, so we don't need to worry about spelling it correctly.</span></span>  <span data-ttu-id="b33db-143">However, if you're using a custom origin, it is *absolutely critical* your hostname is spelled correctly!</span><span class="sxs-lookup"><span data-stu-id="b33db-143">However, if you're using a custom origin, it is *absolutely critical* your hostname is spelled correctly!</span></span>

#### <a name="http-and-https-ports"></a><span data-ttu-id="b33db-144">HTTP and HTTPS ports</span><span class="sxs-lookup"><span data-stu-id="b33db-144">HTTP and HTTPS ports</span></span>
<span data-ttu-id="b33db-145">The other thing to check here is your **HTTP** and **HTTPS ports**.</span><span class="sxs-lookup"><span data-stu-id="b33db-145">The other thing to check here is your **HTTP** and **HTTPS ports**.</span></span>  <span data-ttu-id="b33db-146">In most cases, 80 and 443 are correct, and you will require no changes.</span><span class="sxs-lookup"><span data-stu-id="b33db-146">In most cases, 80 and 443 are correct, and you will require no changes.</span></span>  <span data-ttu-id="b33db-147">However, if the origin server is listening on a different port, that will need to be represented here.</span><span class="sxs-lookup"><span data-stu-id="b33db-147">However, if the origin server is listening on a different port, that will need to be represented here.</span></span>  <span data-ttu-id="b33db-148">If you're not sure, just look at the URL for your origin file.</span><span class="sxs-lookup"><span data-stu-id="b33db-148">If you're not sure, just look at the URL for your origin file.</span></span>  <span data-ttu-id="b33db-149">The HTTP and HTTPS specifications specify ports 80 and 443 as the defaults.</span><span class="sxs-lookup"><span data-stu-id="b33db-149">The HTTP and HTTPS specifications specify ports 80 and 443 as the defaults.</span></span> <span data-ttu-id="b33db-150">In my URL, `https://cdndocdemo.blob.core.windows.net/publicblob/lorem.txt`, a port is not specified, so the default of 443 is assumed and my settings are correct.</span><span class="sxs-lookup"><span data-stu-id="b33db-150">In my URL, `https://cdndocdemo.blob.core.windows.net/publicblob/lorem.txt`, a port is not specified, so the default of 443 is assumed and my settings are correct.</span></span>  

<span data-ttu-id="b33db-151">However, say the URL for your origin file that you tested earlier is `http://www.contoso.com:8080/file.txt`.</span><span class="sxs-lookup"><span data-stu-id="b33db-151">However, say the URL for your origin file that you tested earlier is `http://www.contoso.com:8080/file.txt`.</span></span>  <span data-ttu-id="b33db-152">Note the `:8080` at the end of the hostname segment.</span><span class="sxs-lookup"><span data-stu-id="b33db-152">Note the `:8080` at the end of the hostname segment.</span></span>  <span data-ttu-id="b33db-153">That tells the browser to use port `8080` to connect to the web server at `www.contoso.com`, so you'll need to enter 8080 in the **HTTP port** field.</span><span class="sxs-lookup"><span data-stu-id="b33db-153">That tells the browser to use port `8080` to connect to the web server at `www.contoso.com`, so you'll need to enter 8080 in the **HTTP port** field.</span></span>  <span data-ttu-id="b33db-154">It's important to note that these port settings only affect what port the endpoint uses to retrieve information from the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-154">It's important to note that these port settings only affect what port the endpoint uses to retrieve information from the origin.</span></span>

> [!NOTE]
> <span data-ttu-id="b33db-155">**Azure CDN from Akamai** endpoints do not allow the full TCP port range for origins.</span><span class="sxs-lookup"><span data-stu-id="b33db-155">**Azure CDN from Akamai** endpoints do not allow the full TCP port range for origins.</span></span>  <span data-ttu-id="b33db-156">For a list of origin ports that are not allowed, see [Azure CDN from Akamai Allowed Origin Ports](https://msdn.microsoft.com/library/mt757337.aspx).</span><span class="sxs-lookup"><span data-stu-id="b33db-156">For a list of origin ports that are not allowed, see [Azure CDN from Akamai Allowed Origin Ports](https://msdn.microsoft.com/library/mt757337.aspx).</span></span>  
> 
> 

### <a name="check-the-endpoint-settings"></a><span data-ttu-id="b33db-157">Check the endpoint settings</span><span class="sxs-lookup"><span data-stu-id="b33db-157">Check the endpoint settings</span></span>
<span data-ttu-id="b33db-158">Back on the **Endpoint** blade, click the **Configure** button.</span><span class="sxs-lookup"><span data-stu-id="b33db-158">Back on the **Endpoint** blade, click the **Configure** button.</span></span>

![Endpoint blade with configure button highlighted](https://docstestmedia1.blob.core.windows.net/azure-media/articles/cdn/media/cdn-troubleshoot-endpoint/cdn-endpoint-configure-button.png)

<span data-ttu-id="b33db-160">The endpoint's **Configure** blade appears.</span><span class="sxs-lookup"><span data-stu-id="b33db-160">The endpoint's **Configure** blade appears.</span></span>

![Configure blade](https://docstestmedia1.blob.core.windows.net/azure-media/articles/cdn/media/cdn-troubleshoot-endpoint/cdn-configure.png)

#### <a name="protocols"></a><span data-ttu-id="b33db-162">Protocols</span><span class="sxs-lookup"><span data-stu-id="b33db-162">Protocols</span></span>
<span data-ttu-id="b33db-163">For **Protocols**, verify that the protocol being used by the clients is selected.</span><span class="sxs-lookup"><span data-stu-id="b33db-163">For **Protocols**, verify that the protocol being used by the clients is selected.</span></span>  <span data-ttu-id="b33db-164">The same protocol used by the client will be the one used to access the origin, so it's important to have the origin ports configured correctly in the previous section.</span><span class="sxs-lookup"><span data-stu-id="b33db-164">The same protocol used by the client will be the one used to access the origin, so it's important to have the origin ports configured correctly in the previous section.</span></span>  <span data-ttu-id="b33db-165">The endpoint only listens on the default HTTP and HTTPS ports (80 and 443), regardless of the origin ports.</span><span class="sxs-lookup"><span data-stu-id="b33db-165">The endpoint only listens on the default HTTP and HTTPS ports (80 and 443), regardless of the origin ports.</span></span>

<span data-ttu-id="b33db-166">Let's return to our hypothetical example with `http://www.contoso.com:8080/file.txt`.</span><span class="sxs-lookup"><span data-stu-id="b33db-166">Let's return to our hypothetical example with `http://www.contoso.com:8080/file.txt`.</span></span>  <span data-ttu-id="b33db-167">As you'll remember, Contoso specified `8080` as their HTTP port, but let's also assume they specified `44300` as their HTTPS port.</span><span class="sxs-lookup"><span data-stu-id="b33db-167">As you'll remember, Contoso specified `8080` as their HTTP port, but let's also assume they specified `44300` as their HTTPS port.</span></span>  <span data-ttu-id="b33db-168">If they created an endpoint named `contoso`, their CDN endpoint hostname would be `contoso.azureedge.net`.</span><span class="sxs-lookup"><span data-stu-id="b33db-168">If they created an endpoint named `contoso`, their CDN endpoint hostname would be `contoso.azureedge.net`.</span></span>  <span data-ttu-id="b33db-169">A request for `http://contoso.azureedge.net/file.txt` is an HTTP request, so the endpoint would use HTTP on port 8080 to retrieve it from the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-169">A request for `http://contoso.azureedge.net/file.txt` is an HTTP request, so the endpoint would use HTTP on port 8080 to retrieve it from the origin.</span></span>  <span data-ttu-id="b33db-170">A secure request over HTTPS, `https://contoso.azureedge.net/file.txt`, would cause the endpoint to use HTTPS on port 44300 when retriving the file from the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-170">A secure request over HTTPS, `https://contoso.azureedge.net/file.txt`, would cause the endpoint to use HTTPS on port 44300 when retriving the file from the origin.</span></span>

#### <a name="origin-host-header"></a><span data-ttu-id="b33db-171">Origin host header</span><span class="sxs-lookup"><span data-stu-id="b33db-171">Origin host header</span></span>
<span data-ttu-id="b33db-172">The **Origin host header** is the host header value sent to the origin with each request.</span><span class="sxs-lookup"><span data-stu-id="b33db-172">The **Origin host header** is the host header value sent to the origin with each request.</span></span>  <span data-ttu-id="b33db-173">In most cases, this should be the same as the **Origin hostname** we verified earlier.</span><span class="sxs-lookup"><span data-stu-id="b33db-173">In most cases, this should be the same as the **Origin hostname** we verified earlier.</span></span>  <span data-ttu-id="b33db-174">An incorrect value in this field won't generally cause 404 statuses, but is likely to cause other 4xx statuses, depending on what the origin expects.</span><span class="sxs-lookup"><span data-stu-id="b33db-174">An incorrect value in this field won't generally cause 404 statuses, but is likely to cause other 4xx statuses, depending on what the origin expects.</span></span>

#### <a name="origin-path"></a><span data-ttu-id="b33db-175">Origin path</span><span class="sxs-lookup"><span data-stu-id="b33db-175">Origin path</span></span>
<span data-ttu-id="b33db-176">Lastly, we should verify our **Origin path**.</span><span class="sxs-lookup"><span data-stu-id="b33db-176">Lastly, we should verify our **Origin path**.</span></span>  <span data-ttu-id="b33db-177">By default this is blank.</span><span class="sxs-lookup"><span data-stu-id="b33db-177">By default this is blank.</span></span>  <span data-ttu-id="b33db-178">You should only use this field if you want to narrow the scope of the origin-hosted resources you want to make available on the CDN.</span><span class="sxs-lookup"><span data-stu-id="b33db-178">You should only use this field if you want to narrow the scope of the origin-hosted resources you want to make available on the CDN.</span></span>  

<span data-ttu-id="b33db-179">For example, in my endpoint, I wanted all resources on my storage account to be available, so I left **Origin path** blank.</span><span class="sxs-lookup"><span data-stu-id="b33db-179">For example, in my endpoint, I wanted all resources on my storage account to be available, so I left **Origin path** blank.</span></span>  <span data-ttu-id="b33db-180">This means that a request to `https://cdndocdemo.azureedge.net/publicblob/lorem.txt` results in a connection from my endpoint to `cdndocdemo.core.windows.net` that requests `/publicblob/lorem.txt`.</span><span class="sxs-lookup"><span data-stu-id="b33db-180">This means that a request to `https://cdndocdemo.azureedge.net/publicblob/lorem.txt` results in a connection from my endpoint to `cdndocdemo.core.windows.net` that requests `/publicblob/lorem.txt`.</span></span>  <span data-ttu-id="b33db-181">Likewise, a request for `https://cdndocdemo.azureedge.net/donotcache/status.png` results in the endpoint requesting `/donotcache/status.png` from the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-181">Likewise, a request for `https://cdndocdemo.azureedge.net/donotcache/status.png` results in the endpoint requesting `/donotcache/status.png` from the origin.</span></span>

<span data-ttu-id="b33db-182">But what if I don't want to use the CDN for every path on my origin?</span><span class="sxs-lookup"><span data-stu-id="b33db-182">But what if I don't want to use the CDN for every path on my origin?</span></span>  <span data-ttu-id="b33db-183">Say I only wanted to expose the `publicblob` path.</span><span class="sxs-lookup"><span data-stu-id="b33db-183">Say I only wanted to expose the `publicblob` path.</span></span>  <span data-ttu-id="b33db-184">If I enter */publicblob* in my **Origin path** field, that will cause the endpoint to insert */publicblob* before every request being made to the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-184">If I enter */publicblob* in my **Origin path** field, that will cause the endpoint to insert */publicblob* before every request being made to the origin.</span></span>  <span data-ttu-id="b33db-185">This means that the request for `https://cdndocdemo.azureedge.net/publicblob/lorem.txt` will now actually take the request portion of the URL, `/publicblob/lorem.txt`, and append `/publicblob` to the beginning.</span><span class="sxs-lookup"><span data-stu-id="b33db-185">This means that the request for `https://cdndocdemo.azureedge.net/publicblob/lorem.txt` will now actually take the request portion of the URL, `/publicblob/lorem.txt`, and append `/publicblob` to the beginning.</span></span> <span data-ttu-id="b33db-186">This results in a request for `/publicblob/publicblob/lorem.txt` from the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-186">This results in a request for `/publicblob/publicblob/lorem.txt` from the origin.</span></span>  <span data-ttu-id="b33db-187">If that path doesn't resolve to an actual file, the origin will return a 404 status.</span><span class="sxs-lookup"><span data-stu-id="b33db-187">If that path doesn't resolve to an actual file, the origin will return a 404 status.</span></span>  <span data-ttu-id="b33db-188">The correct URL to retrieve lorem.txt in this example would actually be `https://cdndocdemo.azureedge.net/lorem.txt`.</span><span class="sxs-lookup"><span data-stu-id="b33db-188">The correct URL to retrieve lorem.txt in this example would actually be `https://cdndocdemo.azureedge.net/lorem.txt`.</span></span>  <span data-ttu-id="b33db-189">Note that we don't include the */publicblob* path at all, because the request portion of the URL is `/lorem.txt` and the endpoint adds `/publicblob`, resulting in `/publicblob/lorem.txt` being the request passed to the origin.</span><span class="sxs-lookup"><span data-stu-id="b33db-189">Note that we don't include the */publicblob* path at all, because the request portion of the URL is `/lorem.txt` and the endpoint adds `/publicblob`, resulting in `/publicblob/lorem.txt` being the request passed to the origin.</span></span>






